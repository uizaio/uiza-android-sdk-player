<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uizacoresdk">

    <application
        android:allowBackup="true"
        android:label="@string/app_name">

        <service
            android:name=".view.floatview.FUZVideoService"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".FloatClickFullScreenReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="FLOAT_CLICKED_FULLSCREEN_V3" />
            </intent-filter>
        </receiver>

        <!--START CHROMECAST-->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="uizacoresdk.chromecast.CastOptionsProvider" />
        <activity
            android:name=".chromecast.ExpandedControlsActivity"
            android:theme="@style/Theme.CastVideosDark" />
        <!--STOP CHROMECAST-->
    </application>

</manifest>
